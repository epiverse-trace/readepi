# Get the request query parameters for a specific API version

Get the request query parameters for a specific API version

## Usage

``` r
get_request_params(api_version)
```

## Arguments

- api_version:

  A numeric with the API version

## Value

A data frame with single row containing the query parameters needed to
submit a request
